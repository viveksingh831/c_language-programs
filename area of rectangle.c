#include <stdio.h>
int main(){
    int l,b;
    printf("enter the length and breath of the rectangle");
    scanf("%d%d",&l,&b);
    int area =l*b;
    printf("area of rectangle is %d",area);
return 0;
}
